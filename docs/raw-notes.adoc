[#raw-notes]
= Raw Notes

Words and phrases to look for and destroy:

"simply"
"just"

Look for an verify:

"stream" vs pipeline

Things to verify are consistent:

`.failure` completion
`.finished` completion